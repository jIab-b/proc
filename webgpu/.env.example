# Terrain DSL Configuration
# Copy this file to .env and customize as needed

# Maximum number of chunks to keep loaded in memory
# Higher values = more memory usage but faster terrain access
# Recommended: 256 for large regions, 64 for smaller systems
MAX_LOADED_CHUNKS=256

# View distance in chunks (radius from center)
# Chunks outside this distance will be unloaded
# Recommended: 16 for exploration, 8 for focused work
VIEW_DISTANCE=16

# Default terrain generation parameters
# These are used when not specified in commands
DEFAULT_SEED=1337
DEFAULT_AMPLITUDE=10
DEFAULT_ROUGHNESS=2.4
DEFAULT_ELEVATION=0.35

# Chunk size (blocks per chunk dimension)
# WARNING: Changing this breaks compatibility with existing saves
CHUNK_SIZE=32
