{
  "name": "webgpu-compute",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.38",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
